var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import React, { useState } from "react";
var Input = function (_a) {
    var disabled = _a.disabled, _b = _a.autoComplete, autoComplete = _b === void 0 ? "" : _b, value = _a.value, name = _a.name, onChange = _a.onChange, required = _a.required, _c = _a.autoFocus, autoFocus = _c === void 0 ? false : _c, placeholder = _a.placeholder, _d = _a.maxLength, maxLength = _d === void 0 ? 1024 : _d, save = _a.save, mode = _a.mode, primaryColor = _a.primaryColor;
    var onEnterPress = function (e) {
        if (e.keyCode == 13 && e.shiftKey == false) {
            save && e.preventDefault();
            save && save(e);
        }
    };
    var _e = useState(true), isFocused = _e[0], setFocused = _e[1];
    return (React.createElement("div", { className: "sarufi-field-group", style: {
            marginBottom: "1.5rem",
        } },
        React.createElement("div", { className: "input-group", style: {
                position: "relative",
            } },
            React.createElement("label", { className: "sarufi-flex-start", style: {} },
                React.createElement("textarea", { key: name, onKeyDown: onEnterPress, id: name, autoFocus: autoFocus, name: name, maxLength: maxLength, disabled: disabled, placeholder: placeholder, className: "scroll-bar sarufi-textarea", onChange: onChange, autoComplete: autoComplete, value: value, onFocus: function () { return setFocused(true); }, onBlur: function () { return setFocused(false); }, style: __assign(__assign({ padding: ".7rem .7rem", paddingRight: 45, height: 60, minHeight: 60, maxHeight: 60, borderRadius: "0.3rem", fontFamily: "var(--sarufi-font-family)" }, (mode === "dark"
                        ? {
                            color: "white",
                        }
                        : {})), (mode === "dark" && isFocused
                        ? {
                            borderColor: primaryColor,
                        }
                        : {})), required: required })))));
};
export default Input;
